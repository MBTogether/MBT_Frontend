import React from 'react';
import { BrowserRouter, Route, Routes } from "react-router-dom";


const UserRouter = () => {
    return (
        <Routes>
            {/* <Route path="" component={} /> */} {/* 이런 형식으로 component를 import해와서 path의 주소입력하면 그 페이지로 이동됨 */}
        </Routes>
    );
};

export default UserRouter;